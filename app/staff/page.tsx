import Portal from "../portal/Portal";

export const metadata = { title: "Staff Portal | Laundry Loop", robots: "noindex, nofollow" };

export default function StaffPage() {
  return <Portal portal="staff" />;
}
